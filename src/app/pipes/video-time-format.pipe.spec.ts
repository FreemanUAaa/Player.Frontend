import { VideoTimeFormatPipe } from './video-time-format.pipe';

describe('VideoTimeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new VideoTimeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
